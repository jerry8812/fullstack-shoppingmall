<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.jerry.repository.ImagesRepository">
    <select id="getImagesByTag" parameterType="String" resultType="Images">
        select *
        from images
        where tag = #{imgTag}
    </select>
    <select id="getImageById" parameterType="String" resultType="Images">
        select *
        from images
        where image_id = #{imageId}
    </select>
</mapper>